<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zstu.edu.mapper.DormMapper">

    <select id="getFloor" resultType="java.lang.String">
        select distinct floor from dorm
    </select>
    <select id="getDormitory" resultType="java.lang.String">
        select distinct dormitory from dorm
    </select>
</mapper>
